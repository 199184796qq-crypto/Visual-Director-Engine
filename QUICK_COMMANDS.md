# VDE 简化指令

单张图片可以直接上传给 AI，不需要先建立目录。

## 直接分析参考图

```text
VDE看图：提取这张图的视觉DNA，并生成可复用的提示词。
```

## 参考图改成新画面

```text
VDE改图：保留这张图的构图、光影和色彩，主体改为【新主体】，场景改为【新场景】，输出【目标模型】提示词。
```

## 只借鉴风格

```text
VDE借风格：只提取这张图的色彩、光影、材质和构图，不复制人物、文字或具体物体；为【新画面】生成【目标模型】提示词。
```

## 固定人物连续生成

```text
VDE锁人物：提取图中人物的稳定外貌和服装特征；让他/她出现在【新场景】，保持人物一致，输出【目标模型】提示词。
```

## 风格库沉淀

```text
VDE收录：分析这张图，将可复用的视觉规律整理成风格卡，并标明哪些内容只是图片偶然元素。
```

## 纯文字生图

```text
VDE生图：【主体】在【场景】中，气氛【情绪】，使用【风格或参考图】，输出【目标模型】提示词。
```

## 连续镜头

```text
VDE续镜：沿用上一镜的人物、服装、道具和视觉风格，生成【下一动作或场景】，输出【目标模型】提示词。
```

## 可选参数

如果不写目标模型，默认输出 Universal Visual Prompt；如果写了目标模型，VDE 再转换成对应格式：

- `即梦`
- `Midjourney`
- `SDXL`
- `Flux`
- `H3`

如果不写“只借风格”或“锁人物”，VDE 会根据任务判断是迁移整张参考图的视觉语言，还是只提取其中一部分。

## 最推荐的日常用法

上传图片后只写：

```text
VDE看图：我要把它改成【你的新画面】，输出即梦提示词。
```

如果要避免复制原图内容，写：

```text
VDE借风格：不要复制原图人物和具体物体，只借鉴视觉语言；为【你的新画面】输出即梦提示词。
```
